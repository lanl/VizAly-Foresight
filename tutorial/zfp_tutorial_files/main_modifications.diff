// Add this section before int main(int argc, char *argv[]) at the //compressors include 
#ifdef HAVE_ZFP
    #include "zfpCompressor.hpp"
#endif


// Add this section "// Loop compressors" section just below
// if (compressors[c] == "blosc")
//     compressorMgr = new BLOSCCompressor();
#ifdef HAVE_ZFP
	else if (compressors[c] == "zfp")
		compressorMgr = new ZFPCompressor();
#endif


// Add this section in the // Cycle through params loop just between appendLog(outputLogFile, debuglog); and  MPI_Barrier(MPI_COMM_WORLD);
#ifdef HAVE_ZFP
	if (compressors[c] == "zfp")
	{
		double tolerance = jsonInput["input"]["parameters"]["tolerance"];
		compressorMgr->compressorParameters["tolerance"] = std::to_string(tolerance);
		compressorMgr->compressorParameters["dataType"] = ioMgr->getType();
	}
#endif