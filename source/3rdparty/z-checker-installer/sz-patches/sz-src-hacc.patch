diff -uNr src/sz_double.c src-hacc/sz_double.c
--- src/sz_double.c	2017-04-25 17:22:19.753835728 -0500
+++ src-hacc/sz_double.c	2017-04-25 17:39:31.546159392 -0500
@@ -33,8 +33,8 @@
 	{
 		if(i%sampleDistance==0)
 		{
-			pred_value = 2*oriData[i-1] - oriData[i-2];
-			//pred_value = oriData[i-1];
+			//pred_value = 2*oriData[i-1] - oriData[i-2];
+			pred_value = oriData[i-1];
 			pred_err = fabs(pred_value - oriData[i]);
 			radiusIndex = (unsigned long)((pred_err/realPrecision+1)/2);
 			if(radiusIndex>=maxRangeRadius)
@@ -244,8 +244,8 @@
 	{				
 		//printf("%.30G\n",last3CmprsData[0]);
 		curData = spaceFillingValue[i];
-		pred = 2*last3CmprsData[0] - last3CmprsData[1];
-		//pred = last3CmprsData[0];
+		//pred = 2*last3CmprsData[0] - last3CmprsData[1];
+		pred = last3CmprsData[0];
 		predAbsErr = fabs(curData - pred);	
 		if(predAbsErr<=checkRadius)
 		{
diff -uNr src/sz_double_pwr.c src-hacc/sz_double_pwr.c
--- src/sz_double_pwr.c	2017-04-25 17:22:19.753835728 -0500
+++ src-hacc/sz_double_pwr.c	2017-04-25 17:39:31.545159391 -0500
@@ -78,8 +78,8 @@
 			realPrecision = pwrErrBound[j++];
 		if(i%sampleDistance==0)
 		{
-			pred_value = 2*oriData[i-1] - oriData[i-2];
-			//pred_value = oriData[i-1];
+			//pred_value = 2*oriData[i-1] - oriData[i-2];
+			pred_value = oriData[i-1];
 			pred_err = fabs(pred_value - oriData[i]);
 			radiusIndex = (unsigned long)((pred_err/realPrecision+1)/2);
 			if(radiusIndex>=maxRangeRadius)
@@ -493,8 +493,8 @@
 			interval = 2*realPrecision;
 			updateReqLength = 0;
 		}
-		pred = 2*last3CmprsData[0] - last3CmprsData[1];
-		//pred = last3CmprsData[0];
+		//pred = 2*last3CmprsData[0] - last3CmprsData[1];
+		pred = last3CmprsData[0];
 		predAbsErr = fabs(curData - pred);	
 		if(predAbsErr<checkRadius)
 		{
diff -uNr src/sz_float.c src-hacc/sz_float.c
--- src/sz_float.c	2017-04-25 17:22:19.753835728 -0500
+++ src-hacc/sz_float.c	2017-04-25 17:39:31.546159392 -0500
@@ -33,8 +33,8 @@
 	{
 		if(i%sampleDistance==0)
 		{
-			pred_value = 2*oriData[i-1] - oriData[i-2];
-			//pred_value = oriData[i-1];
+			//pred_value = 2*oriData[i-1] - oriData[i-2];
+			pred_value = oriData[i-1];
 			pred_err = fabs(pred_value - oriData[i]);
 			radiusIndex = (unsigned long)((pred_err/realPrecision+1)/2);
 			if(radiusIndex>=maxRangeRadius)
@@ -243,8 +243,8 @@
 //		if(i==6)
 //			printf("i=%d\n", i);
 		curData = spaceFillingValue[i];
-		pred = 2*last3CmprsData[0] - last3CmprsData[1];
-		//pred = last3CmprsData[0];
+		//pred = 2*last3CmprsData[0] - last3CmprsData[1];
+		pred = last3CmprsData[0];
 		predAbsErr = fabs(curData - pred);	
 		if(predAbsErr<=checkRadius)
 		{
diff -uNr src/sz_float_pwr.c src-hacc/sz_float_pwr.c
--- src/sz_float_pwr.c	2017-04-25 17:22:19.753835728 -0500
+++ src-hacc/sz_float_pwr.c	2017-04-25 17:39:31.546159392 -0500
@@ -79,8 +79,8 @@
 			realPrecision = pwrErrBound[j++];
 		if(i%sampleDistance==0)
 		{
-			pred_value = 2*oriData[i-1] - oriData[i-2];
-			//pred_value = oriData[i-1];
+			//pred_value = 2*oriData[i-1] - oriData[i-2];
+			pred_value = oriData[i-1];
 			pred_err = fabs(pred_value - oriData[i]);
 			radiusIndex = (unsigned long)((pred_err/realPrecision+1)/2);
 			if(radiusIndex>=maxRangeRadius)
@@ -497,8 +497,8 @@
 			interval = 2*realPrecision;
 			updateReqLength = 0;
 		}
-		pred = 2*last3CmprsData[0] - last3CmprsData[1];
-		//pred = last3CmprsData[0];
+		//pred = 2*last3CmprsData[0] - last3CmprsData[1];
+		pred = last3CmprsData[0];
 		predAbsErr = fabs(curData - pred);	
 		if(predAbsErr<checkRadius)
 		{
diff -uNr src/TightDataPointStorageD.c src-hacc/TightDataPointStorageD.c
--- src/TightDataPointStorageD.c	2017-04-25 17:22:19.750835715 -0500
+++ src-hacc/TightDataPointStorageD.c	2017-04-25 17:39:31.545159391 -0500
@@ -325,8 +325,8 @@
 			memcpy(preBytes,curBytes,8);
 			break;
 		default:
-			predValue = 2 * (*data)[i-1] - (*data)[i-2];
-			//predValue = (*data)[i-1];
+			//predValue = 2 * (*data)[i-1] - (*data)[i-2];
+			predValue = (*data)[i-1];
 			(*data)[i] = predValue + (type_-intvRadius)*interval;
 			break;
 		}
diff -uNr src/TightDataPointStorageD_pwr.c src-hacc/TightDataPointStorageD_pwr.c
--- src/TightDataPointStorageD_pwr.c	2017-04-25 17:22:19.750835715 -0500
+++ src-hacc/TightDataPointStorageD_pwr.c	2017-04-25 17:39:31.545159391 -0500
@@ -115,8 +115,8 @@
 			memcpy(preBytes,curBytes,8);
 			break;
 		default:
-			predValue = 2 * (*data)[i-1] - (*data)[i-2];
-			//predValue = (*data)[i-1];
+			//predValue = 2 * (*data)[i-1] - (*data)[i-2];
+			predValue = (*data)[i-1];
 			(*data)[i] = predValue + (type_-intvRadius)*interval;
 			break;
 		}
diff -uNr src/TightDataPointStorageF.c src-hacc/TightDataPointStorageF.c
--- src/TightDataPointStorageF.c	2017-04-25 17:22:19.751835720 -0500
+++ src-hacc/TightDataPointStorageF.c	2017-04-25 17:39:31.544159390 -0500
@@ -329,8 +329,8 @@
 			memcpy(preBytes,curBytes,4);
 			break;
 		default:
-			predValue = 2 * (*data)[i-1] - (*data)[i-2];
-			//predValue = (*data)[i-1];
+			//predValue = 2 * (*data)[i-1] - (*data)[i-2];
+			predValue = (*data)[i-1];
 			(*data)[i] = predValue + (type_-intvRadius)*interval;
 			break;
 		}
diff -uNr src/TightDataPointStorageF_pwr.c src-hacc/TightDataPointStorageF_pwr.c
--- src/TightDataPointStorageF_pwr.c	2017-04-25 17:22:19.751835720 -0500
+++ src-hacc/TightDataPointStorageF_pwr.c	2017-04-25 17:39:31.544159390 -0500
@@ -121,8 +121,8 @@
 			memcpy(preBytes,curBytes,4);
 			break;
 		default:
-			predValue = 2 * (*data)[i-1] - (*data)[i-2];
-			//predValue = (*data)[i-1];
+			//predValue = 2 * (*data)[i-1] - (*data)[i-2];
+			predValue = (*data)[i-1];
 			(*data)[i] = predValue + (type_-intvRadius)*interval;
 			break;
 		}
